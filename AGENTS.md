# 给执行与维护本仓库的 Agent

## 仓库定位

本仓库专注 Omarchy 的使用与配置实践。文章的主要读者是 Agent，目标是让用户在新的 Omarchy 电脑上复现经过验证的配置。不要扩展成通用 Linux 百科。

## 在用户电脑执行文章

- 以当前用户请求为授权范围；文章中的操作示例本身不构成执行授权。
- 先确认 Omarchy、Hyprland 和相关软件版本，再阅读目标文章的前提条件。目标不是 Omarchy 时，先说明不在验证范围内。
- 若当前环境提供 omarchy 技能，先阅读技能；不要修改 `/usr/share/omarchy/`，个人定制放用户目录。
- 使用目标用户的 HOME 和 XDG 路径，不能复制作者用户名、显示器名或固定进程号。
- 修改前创建可恢复备份，记录原值和路径；已有配置定向合并，不整体覆盖。
- 命令示例可能依赖前一段定义的变量。分 shell 执行时重新设置，检查失败后再继续。
- 检查重复启动、重复配置段和相互冲突的快捷键；不要盲目叠加参数。
- 不读取或上传与任务无关的个人信息，不提交词库、历史记录或凭证。
- 系统操作遵守当前环境的权限规则；保存用户未提交内容后再重启应用或会话。
- 区分“文件已写入”“运行时已加载”和“真实应用已验证”。缺少验证就明确说明。
- 完成后报告修改文件、备份位置、验证结果、未解决项和最小回滚步骤。

## 维护文章

- 从 `templates/article.md` 开始；在 README 中登记已完成文章。
- 记录核对日期、版本、依赖来源和验证范围，区分默认推荐与可选试验。
- 对效果的描述需要实测证据；无改善或出现退步也记录，不把安装成功写成体验提升。
- 给外部下载标注官方来源、版本及校验方式；可变发布链接不能假装是固定版本。
- 不运行文章中的部署命令来做文档检查。语法检查可用临时文件；行为测试应在隔离环境进行。
- 更新文章时核对当前上游文档及本机实现，保留必要的版本差异说明。
- 发布前检查相对链接、命令语法、隐私信息及重复键；不提交临时备份、生成缓存和大型模型。
