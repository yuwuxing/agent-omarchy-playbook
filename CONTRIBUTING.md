# 贡献指南

欢迎把 Omarchy 上经过验证的使用技巧整理成 Agent 可执行的文章。

1. 复制 [模板](templates/article.md) 到 `docs/<主题>/<文章>.md`，文件名使用简短英文，正文使用中文。
2. 写清具体问题、适用版本和目标状态，给出检查、备份、配置、验收、回滚的完整路径。
3. 区分个人偏好、推荐配置和可选试验。没有实际验证的内容必须标记，不承诺自动适用所有版本。
4. 配置示例使用 HOME / XDG 路径；只包含复现所需内容，去除个人账号、路径、历史和凭证。
5. 在 README 登记文章，检查相对链接和代码块语法。语法通过不等于实际部署成功。
6. 提交变更时说明解决的问题、验证环境和结果；后续版本失效时补充差异与修复步骤。

大型依赖使用上游链接与校验信息，不把模型文件或系统备份放进 Git。
